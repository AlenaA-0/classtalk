# 课通项目工作日志

## 2026-06-23

- 修复"Failed to fetch"错误：原因是通过file://协议直接打开HTML文件导致浏览器禁止跨域请求
  - 改进app.js和chat.js的代理URL逻辑，添加多URL回退机制
  - 修改server.js CORS头为动态回显Origin（支持file://协议的Origin: null）
  - 确保8080端口服务器正常运行
  - 秘诀：必须通过 http://localhost:8080 访问页面，不要直接双击HTML文件
- 修复百度OCR Secret Key转录错误（从截图OCR引入的字符错误）：
  - 旧值: HmPCIIEPCLFnqj2X0STkjPsWilmZdXJ（OCR误读：大写I→小写l, 小写i→小写l, 单J→双JJ）
  - 正确: HmPCIlEPCLFnqj2X0STkjPsWiImZdXJJ
  - 认证测试成功（Token获取正常）
- 重要教训：截图OCR转录API密钥极易出错，务必从控制台直接复制粘贴
- 用DeepSeek AI替代硬编码解析课表，大幅提升准确性：
  - 在server.js添加 /parse-schedule-proxy 端点，一步完成 OCR→AI解析
  - 删除旧的 parseScheduleText 硬编码课程匹配和 callBaiduOcr 单步调用
  - 新流程：图片→百度OCR识别文字→DeepSeek AI智能解析→结构化课表JSON
  - 添加通用 fetchProxy 函数和 convertAiCourses 转换函数
- 修复DeepSeek API Key无效问题（同样是从截图OCR转录的密钥出错）：
  - 旧值: sk-82abb6b2f50a4c54a2e6bb42de5a70fa（无效，authentication_error）
  - 正确: sk-fbc13ba9b80e4d9987af9640e60e22e0（直接从DeepSeek平台复制粘贴）
  - API认证测试成功，deepseek-v4-flash模型正常返回
  - 再次印证：截图OCR转录API密钥极易出错
- 实现课表保存到localStorage并联动首页本周课表预览：
  - 新增全局变量 currentParsedCourses 存储当前解析结果
  - 新增 renderWeeklyPreview() 函数动态渲染首页课表（替换硬编码HTML）
  - "保存课表"按钮真正保存到localStorage + 更新首页预览 + 自动跳转到首页
  - 页面加载时从localStorage读取已保存课表并显示在首页预览
  - 修复了 bindScheduleButtons 中的语法错误（forEach括号不匹配）
- 添加课程单元格点击详情弹窗：
  - 新增 showCoursePopup/closeCoursePopup/initCoursePopup 函数
  - 弹窗显示课程名、星期、时间段、教室、教师等详细信息
  - "AI课程助手"按钮跳转到AI对话页面并自动填入课程相关问题
  - 课程单元格添加 cursor:pointer 和 hover 缩放效果
  - renderWeeklyPreview 和 renderScheduleGrid 中绑定点击事件
  - 弹窗支持点击遮罩层/关闭按钮/×按钮关闭
- 修复上传按钮不响应问题：
  - 原因：按钮点击事件冒泡到父级upload-area，导致scheduleFileInput.click()被双次调用，浏览器忽略第二次
  - 修复：所有按钮添加 e.stopPropagation() 阻止冒泡
  - 拍照上传按钮改为真正调用文件选择（添加capture=environment属性，移动端会调摄像头）
  - 所有文件输入框添加 value='' 重置，确保重复选择同一文件也能触发change事件
  - 讲义/通知上传区域也做了同样修复
